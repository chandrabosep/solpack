/**
 * @deprecated The file-backed dev store has been replaced by Prisma +
 * Supabase Postgres. See `lib/x402/project-service.ts` and
 * `lib/x402/install-service.ts` for the current data layer.
 *
 * This module is kept only as a stub so older imports don't break the build
 * during migration. Remove it once nothing imports `@/lib/store/file-store`.
 */
export {};
